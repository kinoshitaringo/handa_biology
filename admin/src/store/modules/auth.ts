import { StoreOptions } from "vuex";
// 用户登陆状态

const state = {};
const mutations = {};
const actions = {};
const getters = {};

const auth: StoreOptions<{}> = {
  state,
  mutations,
  actions,
  getters
};

export default auth;
